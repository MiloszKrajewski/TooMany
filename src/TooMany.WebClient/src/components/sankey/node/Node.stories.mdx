import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Node from './Node';

<Meta title="Sankey/Node" component={Node} />

<Canvas>
	<Story name="Basic">
		<Node id="basic">A Basic node</Node>
	</Story>
	<Story name="Basic long name">
		<Node id="basic">A Basic node with quite a bit of text inside of it</Node>
	</Story>
	<Story name="Inputs">
		<Node id="inputs" inputs={['a', 'b', 'c']}>
			Inputs
		</Node>
	</Story>
	<Story name="Outputs">
		<Node id="outputs" outputs={['a', 'b', 'c']}>
			Outputs
		</Node>
	</Story>
	<Story name="Inputs & Outputs">
		<Node
			id="inputs+outputs"
			inputs={['a', 'b', 'c']}
			outputs={['a', 'b', 'c']}
		>
			Inputs & Outputs
		</Node>
	</Story>
</Canvas>
